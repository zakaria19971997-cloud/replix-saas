<!doctype html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ \$title ?? 'Untitled' }}</title>
    @vite('themes/{{ theme_name }}/public/js/app.js')
</head>
<body>
    @yield('content')
</body>
</html>